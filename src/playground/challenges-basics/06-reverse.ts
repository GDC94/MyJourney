// CHALLENGE — Reverse string
// Invertí una palabra. 'kaya' → 'ayak'
// Correr: pnpm tsx src/playground/challenges-basics/06-reverse.ts

function reverseStr(s: string): string {
  // TODO
  return ''
}

console.log(reverseStr('kaya')) // esperado: 'ayak'

// ─── Solución ───────────────────────────────────────────────────────────
// return s.split('').reverse().join('')
