// FOR — Ejercicio 1: Sumar con for clásico
// Sumá todos los números usando un for (sin reduce).
// Correr: pnpm tsx src/playground/for/01-sum.ts

function sumWithFor(arr: number[]): number {
  let total = 0
  // TODO  (for (let i = 0; i < arr.length; i++) ...)
  return total
}

console.log(sumWithFor([5, 10, 15])) // esperado: 30