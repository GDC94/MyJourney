// FOR — Ejercicio 3: for...of
// Uní las palabras separadas por ' - ' usando for...of (sin join).
// Correr: pnpm tsx src/playground/for/03-for-of.ts

function joinWith(arr: string[]): string {
  let result = ''
  // TODO  (for (const w of arr) { ... ojo el primer elemento sin separador })
  return result
}

console.log(joinWith(['uno', 'dos', 'tres'])) // esperado: 'uno - dos - tres'
