// SORT — Ejercicio 3: Strings alfabéticamente
// Ordená las palabras alfabéticamente (case-insensitive correcto).
// Correr: pnpm tsx src/playground/sort/03-strings.ts

function sortAlpha(arr: string[]): string[] {
  // TODO  (localeCompare, NO el operador <)
  return []
}

console.log(sortAlpha(['banana', 'Apple', 'cherry', 'apple']))
// esperado: ['apple', 'Apple', 'banana', 'cherry']