// SORT — Ejercicio 1: Números
// Ordená los números de mayor a menor, SIN mutar el original.
// Correr: pnpm tsx src/playground/sort/01-numbers.ts

function sortDesc(arr: number[]): number[] {
  // TODO  (toSorted con el comparador correcto)
  return []
}

const original = [5, 2, 8, 1, 9]
console.log(sortDesc(original)) // esperado: [9, 8, 5, 2, 1]
console.log(original) // esperado: [5, 2, 8, 1, 9] ← NO debe mutarse
