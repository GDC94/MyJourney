// FILTER — Ejercicio 1: Solo pares
// Dado [1,2,3,4,5,6], devolvé solo los números pares.
// Correr: pnpm tsx src/playground/filter/01-evens.ts

function evens(arr: number[]): number[] {
  // TODO
  return []
}

console.log(evens([1, 2, 3, 4, 5, 6])) // esperado: [2, 4, 6]

// ─── Solución ───────────────────────────────────────────────────────────
// return arr.filter((n) => n % 2 === 0)
